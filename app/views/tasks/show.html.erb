<div class="container">
  <div class="page-container">
    <header>
      <div class="top-camera-gap"></div>
      <div class="content-center">
        <div class="header-label">Progress</div>
        <span class="vertical-menu-btn" onClick="menuOption();">
          <!-- Dropdown content -->
          <div id="login_dropdown_content">
            <ul>
              <li>
                <div class="content-left mt-2">
                  <div class="img-contact-us"></div>
                  <span>Contact with us</span>
                </div>
              </li>
              <li>
                <div class="content-left mt-2">
                  <div class="img-faq"></div>
                  <span>FAQ</span>
                </div>
              </li>
              <li>
                <div class="content-left mt-2">
                  <%= form_with url: destroy_user_session_path, method: :delete, local: true do %>
                    <%= submit_tag 'Logout', class: 'btn btn-link', style: 'padding: 0; color: inherit; text-decoration: none;' %>
                  <% end %>
                </div>
              </li>
            </ul>
          </div>
        </span>
      </div>

      <div class="footer-img-box content-center">
        <div class="new-mountain-img"></div>
      </div>
    </header>

    <section>
      <article>

        <div>
          <div class="content-center label-text-color fs-4 fw-bold mt-3">
            <div>
              <div style="text-align:center;"> <%= @goal.name %> </div>
              <div style="font-weight: 100; font-size: 1.3rem;text-align:center; margin-bottom: 1rem;"> <%= @task.name %> </div>
            </div>
          </div>
        </div>

        <div class="box-underline">
          <div style="display: inline-block; margin: 4px;">
            <input type="checkbox" <%= @task.completed ? 'checked' : '' %> disabled />
          </div>

          <div style="display: inline-block; width: 91%;">
            <div class="two-elements">
              <span class="custom-form-label" id="task_deadline"> <%= @task.deadline.present? ? @task.deadline.strftime("%d %b %Y") : "No deadline" %> </span>
              <span class="time-left-field del-task"> </span>
            </div>

            <div class="two-elements">
              <span id="task_name" class="del-task" style="text-decoration: none;"> <%= @task.name %> </span>
              <span class="del-task" style="text-decoration: none;"> <%= @task.completed ? "Compleated" : "Not Compleated" %> </span>
            </div>
          </div>
        </div>


        <div>
          <!--Task information -->
          <div style="margin-top: 2rem;">
            <div class="two-elements show-info-field">
              <span>Task progress:</span>
              <span id="task_progress" style="padding-right: 1rem;"></span>
            </div>

            <div class="two-elements show-info-field">
              <span>Task start date:</span>
              <span>
                  <input type="date" style="text-align: right;
                                            border: none;
                                            font-size: 1rem;
                                            color: #787878;
                                            background-color: #fff;" id="task_start_date" disabled>
              </span>
            </div>

            <div class="two-elements show-info-field">
              <span>Task end date:</span>
              <span>
                <input type="date" style="text-align: right;
                                              border: none;
                                              font-size: 1rem;
                                              color: #787878;
                                              background-color: #fff;" id="task_end_date" disabled>
              </span>
            </div>
          </div>

          <!-- Task menu Buttons -->
          <div class="content-center" style="margin-top: 2rem;">
            <div class="delete-btn"><%= link_to 'Delete Task', goal_task_path(@goal, @task), method: :delete, data: { turbo_confirm: "Are you sure?" } %></div>
            <div class="edit-btn"><%= link_to 'Edit Task', edit_goal_path(@goal) %></div>
            <div class="done-btn"> </div>
          </div>
        <div>

        <!--
        <div class="task-details">
          <div class="list-content-box" id="list-content-box-task-show">
            <div class="list-content-claster mountain-label" style="width: 5%;">
              <input type="checkbox" <%= @task.completed ? 'checked' : '' %> disabled />
            </div>
            <div class="list-content-claster mountain-label" style="width: 95%;">
              <p>Deadline: <%= @task.deadline.present? ? @task.deadline.strftime("%d %b %Y") : "No deadline" %></p>
              <p>Completed: <%= @task.completed ? "yes" : "no" %></p>
            </div>
          </div>
        -->

          <div class="task-actions">
            <!--
            <%= link_to 'Edit Task', edit_goal_path(@goal), class: 'btn btn-small' %>
            <%= link_to 'Delete Task', goal_task_path(@goal, @task), method: :delete, data: { turbo_confirm: "Are you sure?" }, class: "btn btn-small delete-task" %>
            <%= link_to 'Back to Goal', goal_path(@goal), class: 'btn btn-small' %>
            -->
          </div>
        </div>
      </article>
    </section>
  </div>

  <footer>
    <div class="nav">
      <div class="nav-box">
        <a href="/goals">
          <div class="all-mountains-btn"></div>
        </a>
      </div>
      <div class="nav-box">
        <a href="#">
          <!--Need make link without name just <a href.... -->
        <!--<%= link_to 'Back to Goal', goal_path(@goal) %> -->
          <div class="back-btn"></div>
        </a>
      </div>
      <div class="nav-box">
        <a href="/profile">
          <div class="profile-btn"></div>
        </a>
      </div>
    </div>
  </footer>
</div>

<!-- Custom confirmation modal -->
<div id="confirmationModal" class="modal">
  <div class="modal-content">
    <span class="close">&times;</span>
    <p>Are you sure you want to delete this task?</p>
    <div class="modal-buttons">
      <button id="confirmDelete" class="btn btn-small">Yes</button>
      <button id="cancelDelete" class="btn btn-small">No</button>
    </div>
  </div>
</div>

<script>
  document.addEventListener('DOMContentLoaded', calculateProgress () );
</script>








<!-- Very bad style put in one file  JavaScript and CSS in one file. Don't mix all in one.
    If you need script which will execute after load this page just add function <script> function() </script>
    and keep JS in other file. -->
<script>
  document.addEventListener('turbo:load', initializeDeleteModal);
  document.addEventListener('DOMContentLoaded', initializeDeleteModal);

  function initializeDeleteModal() {
    var modal = document.getElementById("confirmationModal");
    var confirmButton = document.getElementById("confirmDelete");
    var cancelButton = document.getElementById("cancelDelete");
    var closeButton = document.querySelector(".close");
    var deleteLink;

    document.querySelectorAll('.delete-task').forEach(function(link) {
      link.addEventListener('click', function(event) {
        event.preventDefault();
        deleteLink = link;
        modal.style.display = "block";
      });
    });

    confirmButton.addEventListener('click', function() {
      modal.style.display = "none";
      var form = document.createElement('form');
      form.method = 'post';
      form.action = deleteLink.href;

      var methodInput = document.createElement('input');
      methodInput.type = 'hidden';
      methodInput.name = '_method';
      methodInput.value = 'delete';
      form.appendChild(methodInput);

      var csrfToken = document.querySelector('meta[name="csrf-token"]').content;
      var csrfInput = document.createElement('input');
      csrfInput.type = 'hidden';
      csrfInput.name = 'authenticity_token';
      csrfInput.value = csrfToken;
      form.appendChild(csrfInput);

      document.body.appendChild(form);
      form.submit();
    });

    cancelButton.addEventListener('click', function() {
      modal.style.display = "none";
    });

    closeButton.addEventListener('click', function() {
      modal.style.display = "none";
    });

    window.addEventListener('click', function(event) {
      if (event.target == modal) {
        modal.style.display = "none";
      }
    });
  }
</script>

<style>
  .modal {
    display: none;
    position: fixed;
    z-index: 1;
    left: 0;
    top: 0;
    width: 100%;
    height: 100%;
    overflow: auto;
    background-color: rgba(0, 0, 0, 0.5);
  }

  .modal-content {
    background-color: #fefefe;
    margin: 15% auto;
    padding: 20px;
    border: 1px solid #888;
    width: 300px;
    border-radius: 10px;
    text-align: center;
  }

  .close {
    color: #aaa;
    float: right;
    font-size: 28px;
    font-weight: bold;
    cursor: pointer;
  }

  .close:hover,
  .close:focus {
    color: black;
    text-decoration: none;
  }

  .modal-buttons {
    display: flex;
    justify-content: space-around;
    margin-top: 20px;
  }

  .btn {
    padding: 10px 20px;
    border: none;
    border-radius: 5px;
    cursor: pointer;
    font-size: 16px;
  }

  .btn-small {
    font-size: 14px;
  }
</style>
